# auth — components

Presentational + form components for the auth feature (sign-up, sign-in).
One component per file, kebab-case, arrow functions, named exports.
Consumes shadcn primitives from `@/components/ui`.
